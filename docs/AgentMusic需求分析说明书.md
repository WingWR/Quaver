# AgentMusic 软件需求规格说明书（SRS）

版本：V1.1  
日期：2026-04-02  
项目名称：AgentMusic  
文档类型：Software Requirements Specification

## 1. 引言

### 1.1 编写目的

本文档用于明确 AgentMusic 的需求边界、功能范围、约束条件、异常场景、验收标准与当前实现状态，作为课程设计开发、测试、答辩与后续需求变更的基准文档。

本文档面向以下对象：

- 项目开发者
- 课程指导教师
- 测试与验收人员

### 1.2 项目背景

AgentMusic 是一个 Web 端 AI 音乐 Agent 系统。系统目标不是实现一个普通音乐播放器，而是实现一个“能通过自然语言理解用户需求，并完成推荐、歌单生成、播放控制与状态同步”的智能音乐助手。

系统拟解决的核心问题如下：

- 传统音乐播放器中，用户需要通过多次点击完成搜索、筛选、播放和切换操作，交互成本高。
- 平台推荐结果与用户即时场景需求存在偏差，缺少对“当前任务、情绪、语言、场景”的即时理解。
- 用户缺少对 AI 推荐结果的版本化管理能力，难以回看历史推荐结果。

### 1.3 术语定义

- Agent：系统中的 AI 智能代理，负责理解用户输入并触发推荐或控制行为。
- Bridge Spotify Account：开发阶段用于桥接 Spotify API 的开发者 Spotify 账号。
- 播放会话：用户当前播放状态的本地表示，包括当前曲目、进度、播放模式、设备等。
- 推荐歌单版本：Agent 针对同一用户在不同时间生成的多份推荐结果。

### 1.4 参考资料

- 项目选题与 Proposal 文档
- 数据库结构设计文档
- 项目开发笔记
- Spotify Web API 官方文档
- Roger Pressman 软件工程教材中关于 SRS 的结构化要求

## 2. 总体描述

### 2.1 产品定位

AgentMusic 是一个“聊天驱动的音乐软件”。核心交互不是传统播放器的菜单与按钮，而是：

- 用户通过文本或语音表达需求。
- Agent 解析需求并生成推荐或控制动作。
- 系统以聊天回复、推荐歌单、播放状态三种形式反馈结果。

### 2.2 产品目标

当前版本目标如下：

1. 支持用户通过聊天方式请求推荐歌曲或歌单。
2. 支持系统生成可保存、可回看的推荐歌单版本。
3. 支持底部播放器展示和同步当前播放状态。
4. 支持通过 Spotify API 获取基础音乐元数据与播放控制能力。
5. 实现前端界面、后端接口、Spotify 接口三层之间的基本联通。

### 2.3 产品边界

#### 2.3.1 本版本要做的内容

- Web 前端聊天界面
- Web 前端音乐主界面
- 左侧推荐歌单历史区
- 底部播放器状态展示与基础控制
- 后端 Agent 聊天接口
- 后端歌单与播放会话接口
- Spotify bridge 模式下的鉴权、元数据读取与基础播放控制

#### 2.3.2 本版本不做的内容

- 多租户商业化账号体系
- 用户个人 Spotify 账号绑定流程的完整产品化实现
- 完整歌词服务
- 大规模并发部署与生产级运维体系
- 移动端原生 App
- 社区分享、评论、关注等社交功能

### 2.4 需求优先级

#### 2.4.1 Priority 1

- 聊天主界面
- 推荐歌单生成与展示
- 历史歌单读取
- 底部播放器状态同步
- 播放、暂停、上一首、下一首、进度拖动、播放模式切换
- 曲目、歌手、搜索等音乐元数据查询

#### 2.4.2 Priority 2

- 设备列表展示
- 设备切换
- 语音识别完整链路
- 流式聊天输出
- 用户个人 Spotify 账号绑定产品化
- MySQL/Redis 持久化完整替换内存实现

#### 2.4.3 Priority 3

- 社区能力
- 商业化账号体系
- 移动端原生应用
- 多端同步优化

### 2.5 用户角色

#### 2.5.1 普通用户

能力：

- 通过聊天请求推荐歌曲或歌单
- 查看历史推荐歌单
- 查看当前播放状态
- 使用底部播放器基础控制

限制：

- 不能访问 bridge Spotify 账号的私有设备列表、收藏库、历史播放数据

#### 2.5.2 系统管理员/开发者

能力：

- 配置 OpenAI/Kimi/Spotify 等外部服务参数
- 配置 bridge Spotify 账号
- 维护系统默认设备、授权回调地址和调试配置

#### 2.5.3 第三方系统

当前涉及：

- Spotify Web API
- OpenAI API 或兼容 LLM API

### 2.6 运行环境

- 前端：Web 浏览器，桌面优先，兼容主流 Chromium 浏览器
- 后端：Java 21 + Spring Boot
- 前端框架：React + Vite
- 数据存储：MySQL + Redis，当前部分实现仍为内存过渡态

## 3. 系统场景描述

### 3.1 场景一：用户请求推荐歌单并立即播放

流程：

1. 用户进入聊天主界面。
2. 用户输入“给我推荐适合深夜学习的粤语歌，并直接播放”。
3. Agent 解析用户需求。
4. 系统读取用户偏好、聊天上下文与历史推荐结果。
5. 系统生成推荐歌单。
6. 系统保存该推荐歌单为一个新版本。
7. 系统同步更新当前播放会话。
8. 聊天区显示回复。
9. 左侧歌单区出现新的推荐版本。
10. 底部播放器显示当前播放曲目。

### 3.2 场景二：用户只请求推荐，不立即播放

流程：

1. 用户输入“先生成一个雨天通勤歌单，不要播放”。
2. Agent 识别为 recommend-only 场景。
3. 系统生成并保存歌单。
4. 聊天区返回推荐结果。
5. 左侧歌单区刷新。
6. 播放器状态不变。

### 3.3 场景三：用户查看历史推荐结果

流程：

1. 用户查看左侧历史推荐歌单列表。
2. 用户点击某个历史版本。
3. 系统读取该歌单及曲目详情。
4. 页面展示该歌单内容。

### 3.4 场景四：用户控制当前播放

流程：

1. 用户点击底部播放器的播放、暂停、上一首、下一首、模式切换或进度拖动。
2. 前端调用后端 playback 接口。
3. 后端调用 Spotify 播放控制接口。
4. 播放会话更新。
5. 前端刷新播放器状态。

## 4. 外部接口需求

### 4.1 用户接口

#### 4.1.1 聊天主界面

应提供以下 UI 元素：

- 聊天消息流
- 单行起始、自动增高的输入框
- 回车发送、Shift+回车换行
- 语音输入按钮
- 空状态提示词

#### 4.1.2 左侧侧栏

应提供以下功能：

- 页面主导航
- 推荐歌单历史列表
- 后续可扩展的收藏或快捷入口

#### 4.1.3 底部播放器

应提供以下功能：

- 当前曲目展示
- 当前歌手名展示
- 播放/暂停
- 上一首/下一首
- 进度条
- 音量条
- 播放模式展示与切换

以下能力后置到 Priority 2，不纳入当前最小闭环验收：

- 设备列表展示
- 设备切换

### 4.2 软件接口

#### 4.2.1 前后端接口

当前版本至少包括：

- `POST /api/agent/chat`
- `GET /api/agent/history/{userId}`
- `GET /api/playlists/{userId}`
- `GET /api/playback/{userId}/session`
- `PUT /api/playback/{userId}/session`
- `GET /api/music/tracks/{trackId}`
- `GET /api/music/artists/{artistId}`
- `GET /api/music/search/tracks`

#### 4.2.2 外部 API 接口

- Spotify Web API
- OpenAI API 或 OpenAI 兼容 API

### 4.3 通信接口

- 前后端：HTTP/JSON
- 浏览器与后端：本地开发阶段使用 `http://localhost`
- 后续实时状态可扩展为 WebSocket 或 SSE，但不作为当前版本强制要求

## 5. 功能需求

### 5.1 用户上下文管理

#### 5.1.1 需求描述

系统应能够基于用户标识维护聊天记录、推荐歌单和播放会话。

#### 5.1.2 约束

- 当前版本可使用固定测试用户 `demo-user`
- 后续再扩展为正式用户体系

#### 5.1.3 验收标准

- 使用同一用户标识连续请求时，可读取该用户历史聊天与歌单

### 5.2 聊天请求处理

#### 5.2.1 需求描述

系统应允许用户通过自然语言输入推荐需求或播放控制需求。

#### 5.2.2 输入

- 文本消息
- 预留语音输入入口

#### 5.2.3 输出

- Agent 回复文本
- 可选推荐歌单结果
- 可选播放会话结果

#### 5.2.4 验收标准

- 用户发送消息后，前端 3 秒内收到后端响应
- 回复中包含意图标识或可解释结果

### 5.3 推荐歌单生成

#### 5.3.1 需求描述

系统应基于用户自然语言、用户偏好、历史聊天与历史歌单生成推荐歌单。

#### 5.3.2 业务规则

- 每次生成新推荐歌单时，保存为新版本
- 每个用户默认保留最近 10 个推荐版本
- 如果用户明确要求“不播放”，则只生成歌单，不触发播放

#### 5.3.3 验收标准

- 推荐请求成功后，左侧侧栏出现新的推荐版本
- 新歌单中至少包含 1 首可展示曲目

### 5.4 历史歌单管理

#### 5.4.1 需求描述

系统应支持用户查看历史推荐歌单，并在侧栏展示。

#### 5.4.2 验收标准

- 用户刷新页面后仍可看到最近生成的历史歌单
- 至少支持展示最近 10 个版本

### 5.5 底部播放器状态同步

#### 5.5.1 需求描述

系统应能在底部播放器展示当前播放会话的真实状态。

#### 5.5.2 包含字段

- 当前曲目
- 当前歌手名
- 当前进度
- 是否正在播放
- 播放模式
- 设备标识

#### 5.5.3 验收标准

- 用户进入页面后，播放器能读取当前会话
- 若当前会话存在曲目，则播放器显示对应曲目信息与歌手名

### 5.6 播放控制

#### 5.6.1 需求描述

系统应支持以下基础播放控制：

- 播放
- 暂停
- 上一首
- 下一首
- 切换播放模式
- 进度拖动

明确后置到 Priority 2 的能力：

- 设备列表展示
- 设备切换

#### 5.6.2 前提条件

- Spotify bridge 账号已授权
- 对应设备可被 Spotify 识别
- 账号具备满足当前能力的服务条件

#### 5.6.3 验收标准

- 前端触发操作后，后端播放会话状态发生变化
- 前端播放器状态与后端会话保持一致

### 5.7 音乐元数据查询

#### 5.7.1 需求描述

系统应支持查询以下信息：

- 曲目信息
- 歌手信息
- 搜索结果

#### 5.7.2 验收标准

- 查询请求返回结构化 JSON
- 前端能够展示曲目标题、歌手名、封面、时长等基础字段

### 5.8 语音输入入口

#### 5.8.1 需求描述

前端应保留语音输入 UI 入口。

#### 5.8.2 当前版本范围

- 当前版本只要求保留入口与交互提示
- 不要求完成完整语音识别链路

#### 5.8.3 验收标准

- 页面上可见语音输入按钮
- 悬浮提示说明其用途

## 6. 非功能需求

### 6.1 性能需求

- 聊天接口在本地开发环境下平均响应时间应小于 3 秒
- 非流式条件下，历史聊天读取应在 1 秒内完成
- 侧栏歌单列表读取应在 1 秒内完成
- 播放会话读取应在 1 秒内完成

### 6.2 可用性需求

- 聊天输入框支持回车发送
- 输入框为空时居中展示，有消息后固定在消息流底部
- 页面主要操作在桌面浏览器中应可通过鼠标直接完成

### 6.3 可靠性需求

- 外部 API 调用失败时，系统必须返回可理解的错误信息
- Spotify 或 LLM 调用失败时，不得导致前端页面崩溃

### 6.4 安全需求

- OpenAI API Key、Spotify Client Secret 不得提交入仓库
- bridge Spotify 账号的私有资源不得直接暴露给普通用户
- 用户密码若后续实现，必须使用哈希存储

### 6.5 合规与版权约束

- 系统不得非法存储完整 Spotify 音频内容
- 当前测试期允许使用最小静态测试音源，但正式版本应移除不合规第三方音频源

## 7. 约束条件

### 7.1 平台约束

- 当前仅支持 Web 端
- 当前开发环境以 Windows + 浏览器为主

### 7.2 第三方依赖约束

- 依赖 Spotify Web API
- 依赖 OpenAI API 或兼容服务
- 外部网络不可达时，部分功能无法工作

### 7.3 账户与服务约束

- 播放控制能力依赖 bridge Spotify 账号可用
- 若 bridge Spotify 账号无法授权或设备不可用，播放器真实控制链路无法验证

### 7.4 开发约束

- 当前项目处于课程设计阶段，优先保证功能闭环而非商业级完善度
- 非关键功能应后置，避免范围蔓延

## 8. 异常场景需求

### 8.1 LLM 不可用

处理要求：

- 系统返回本地 fallback 回复
- 前端显示可理解错误提示

验收标准：

- 页面不崩溃
- 后端返回有效 JSON

### 8.2 Spotify 接口调用失败

处理要求：

- 返回错误信息
- 保留当前本地播放会话状态

验收标准：

- 前端播放器不出现空白或崩溃

### 8.3 无历史歌单

处理要求：

- 左侧侧栏显示“还没有生成过推荐歌单”

验收标准：

- 用户界面无空白异常

### 8.4 播放会话为空

处理要求：

- 底部播放器展示兜底状态
- 不自动报错

### 8.5 网络断开或跨域异常

处理要求：

- 前端提示请求失败
- 页面保留当前已渲染内容

## 9. 验收标准总表

### 9.1 最小可验收版本

满足以下条件即视为当前阶段可验收：

1. 聊天页可以向后端发送消息并获得回复
2. 左侧侧栏可以展示后端返回的历史推荐歌单
3. 底部播放器可以读取后端播放会话
4. 后端接口文档与实际 controller 保持一致
5. 开发笔记与实际开发状态同步

### 9.2 里程碑验收

#### M1：前后端联通

- 聊天、歌单、播放会话接口全部联通

#### M2：推荐闭环

- 用户发送推荐请求后，生成歌单、保存历史、刷新侧栏

#### M3：播放器闭环

- 前端控制动作触发后端 API，后端播放会话同步更新

#### M4：Spotify 闭环

- Spotify bridge 鉴权、元数据查询、播放控制完整联通

## 10. 需求变更机制

任何需求变更必须记录以下内容：

- 变更原因
- 变更内容
- 影响模块
- 预计工期变化
- 是否影响原有验收标准
- 最终确认人

未经记录与确认的口头需求，不作为正式需求执行。

## 11. 当前版本结论

AgentMusic 当前版本的核心不是“做一个完整 Spotify 克隆”，而是验证以下闭环：

- 用户用自然语言表达音乐需求
- Agent 生成推荐或控制决策
- 系统保存推荐结果与播放状态
- 前端以聊天、歌单、播放器三块区域展示结果

凡是不直接服务上述闭环的功能，均应后置。

## 12. 需求追踪表

| 编号 | 需求项 | 优先级 | 当前状态 | 对应实现/说明 | 验收状态 |
| --- | --- | --- | --- | --- | --- |
| R1 | 聊天主页可发送消息并显示 Agent 回复 | P1 | 已实现 | 前端聊天页已接 `POST /api/agent/chat` | 待验证 |
| R2 | 聊天历史读取 | P1 | 已实现 | 前端已接 `GET /api/agent/history/{userId}` | 待验证 |
| R3 | 推荐歌单生成 | P1 | 部分实现 | 后端已具备 planner + playlist 生成链路，推荐质量仍需继续优化 | 待验证 |
| R4 | 左侧历史推荐歌单展示 | P1 | 已实现 | 前端侧栏已接 `GET /api/playlists/{userId}` | 待验证 |
| R5 | 推荐歌单版本化保存 | P1 | 已实现 | 后端歌单服务与历史版本列表已联通 | 待验证 |
| R6 | 底部播放器读取播放会话 | P1 | 已实现 | 前端已接 `GET /api/playback/{userId}/session` | 待验证 |
| R7 | 底部播放器显示真实曲目名与歌手名 | P1 | 已实现 | 曲目详情与歌手详情联查后展示真实名称 | 待验证 |
| R8 | 播放/暂停/上一首/下一首/进度拖动/播放模式切换 | P1 | 部分实现 | 前端控制链路已接后端，仍依赖 Spotify bridge 可用性验证 | 待验证 |
| R9 | 音乐元数据查询（曲目/歌手/搜索） | P1 | 已实现 | 后端 `MusicQueryController` 与 Spotify catalog 调用链已存在 | 待验证 |
| R10 | Spotify bridge 鉴权与 token 刷新 | P1 | 已实现 | 后端 bridge auth 与 token 刷新已实现 | 待验证 |
| R11 | 普通聊天真实 LLM 调用 | P2 | 部分实现 | 已有可开关 live-LLM 分支，但当前受外部网络限制 | 待验证 |
| R12 | 语音输入 UI 入口 | P1 | 已实现 | 前端聊天页已保留语音按钮与提示 | 已验证 |
| R13 | 语音识别完整链路 | P2 | 未实现 | 当前版本只保留 UI 入口 | 未验证 |
| R14 | 设备列表展示 | P2 | 未实现 | 明确后置，不纳入当前最小闭环 | 未验证 |
| R15 | 设备切换 | P2 | 未实现 | 明确后置，不纳入当前最小闭环 | 未验证 |
| R16 | MySQL/Redis 持久化完整替换内存实现 | P2 | 未实现 | 当前仍有内存实现用于开发联调 | 未验证 |
| R17 | 流式聊天输出 | P2 | 未实现 | 当前为同步完整响应，后续可基于 SSE 扩展 | 未验证 |
